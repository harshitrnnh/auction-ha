// Auction rules — single source of truth. Frontend reads these from the API response.
export const MIN_INCREMENT = 1; // ₹ per bid step
export const STARTING_BID = 1;  // ₹ opening price for each lot
