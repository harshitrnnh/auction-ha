// Auction rules — single source of truth. Frontend reads these from the API response.
export const MIN_INCREMENT = 50; // ₹ per bid step
export const STARTING_BID = 700;  // ₹ opening price for each lot
